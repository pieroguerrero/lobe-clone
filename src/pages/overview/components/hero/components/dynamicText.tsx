interface Props {
  play: boolean;
  speedFactor: number;
  /**
   * Indicates the duration in milliseconds of the changing period.
   */
  periodDuration: number;
}
export default function DynamicText({
  play,
  speedFactor,
  periodDuration,
}: Props) {
  return <>feel emotions</>;
}
